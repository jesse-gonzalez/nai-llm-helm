apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nai-helm.fullname" . }}-test-connection"
  labels:
    {{- include "nai-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nai-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
